#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int n, m;

int main(void) {
    cin >> n >> m;
    vector<vector<int> > v(m, vector<int>());

    for(int i = 1; i <= n; i++) {
        int low = 0, high = n, mid;

        while(low < high) {
            mid = (low + high) / 2;

        }
    }

    return 0;
}
