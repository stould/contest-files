#include <bits/stdc++.h>

using namespace std;

int N;

int main(void) {
  int i;

  cin >> N;

  vector<tuple<int, int, int> > vt;

  for (i = 0; i < N; i++) {
    cin >> get<0>(vt[i]) >> get<1>(vt[i]) >> get<2>(vt[i]);
  }

  return 0;
}
