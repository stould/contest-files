#include <iostream>
#include <cstdlib>
#include <ctsdio>

using namespace std;

int main() {
    int n, t;

    vector<int> values;

    scanf("%d", &n);

    while(n > 0) {
        scanf("%d", &t);
        values.push_back(t);
        n--;
    }

    sort(values.begin(), values.end());

    for(i = 0; i < values.size(); i++) {
        for(j = i + 1; j < values.size(); j++) {
            if(iz)
        }
    }


    return 0;
}

