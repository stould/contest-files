T = input();

for _ in range(1, T + 1):
    line = input().split('')

    L = line[0]
    N = int(line[1])

    sz = 0
    bf = 0
    for x in range(1):
        if bf > N:
            break
        bf += pow(len(L), x)
        sz += 1
