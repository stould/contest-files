#include <iostream>
#include <vector>
#include <algorithm>
#include <stdio.h>

using namespace std;

int T, K, N, v[110];

int main(void) {
    cin >> T;

    while(T > 0) {
        cin >> N >> K;
        for(int i = 0; i < N; i++) {
            cin >> v[i];
        }
        while()
        T--;
    }

    return 0;
}
