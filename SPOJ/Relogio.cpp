#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

using namespace std;

string s;
int h, m, s, c, dh, dm, ds, dc;

int main(void) {
    while(getline(s)) {
        h = atoi(s.substr(0, 3));
        m = atoi(s.substr(2, 3));
        s = atoi(s.substr(4, 3));
        c = atoi(s.substr(6, 3));

        int tot = ()
    }
    return 0;
}
