#include <iostream>

using namespace std;

int x, y, n, aux;

int main(void) {
    while(cin >> x >> y >> n && x > 0 && y > 0 && x > 0) {
        int tmp = 0;
        for(i = 0; i < n; i++) {
            cin >> aux; tmp += aux;
        }

    }
    return 0;
}
