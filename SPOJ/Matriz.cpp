#include <iostream>

using namespace std;

typedef long long ll;

int n, p, q, r, s, x, y, ni, nj;

int main(void) {
    cin >> n >> p >> q >> r >> s >> x >> y >> ni >> nj;
    for(i = 1; i <= n; i++) {

    }
    cout << ans << endl;
    return 0;
}

