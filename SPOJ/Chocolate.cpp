#include <iostream>
#include <set>

using namespace std;

int n, m;

int main(void) {
    cin >> n >> m;
    set<int> mem;

    while(n > 0 {
        for(int i = 1; i <= m; i++) {
            if(!count(mem.begin(), mem.end(), i)) {

            }
        }
    }
    return 0;
}
